apiVersion: apps/v1
kind: Deployment
metadata:
  name: products
  namespace: staging
  labels:
    app: products
    environment: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: products
        environment: staging
    spec:
      containers:
        - name: products
          image: aeyzaguirre/products-ecommerce:latest
          ports:
            - containerPort: 3001
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: DB_HOST
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secret
                  key: DB_PASSWORD
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: staging
  labels:
    app: products
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: products
  ports:
    - name: http
      port: 3001
      targetPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: products-nodeport
  namespace: staging
  labels:
    app: products
    environment: staging
spec:
  type: NodePort
  selector:
    app: products
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30011
