apiVersion: apps/v1
kind: Deployment
metadata:
  name: products
  namespace: dev
  labels:
    app: products
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: products
        environment: dev
    spec:
      containers:
        - name: products
          image: aeyzaguirre/products-ecommerce:dev
          ports:
            - containerPort: 3001
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: DB_HOST
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: ecommerce-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secret
                  key: DB_PASSWORD
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
---
# ClusterIP — used by the orders service internally (http://product-service:3001)
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: dev
  labels:
    app: products
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: products
  ports:
    - name: http
      port: 3001
      targetPort: 3001
---
# NodePort — used for external access (e.g. curl, browser, Jenkins validation)
apiVersion: v1
kind: Service
metadata:
  name: products-nodeport
  namespace: dev
  labels:
    app: products
    environment: dev
spec:
  type: NodePort
  selector:
    app: products
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
